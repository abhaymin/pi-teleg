{
  "name": "teleg-bridge-mcp",
  "version": "2.0.0",
  "description": "Telegram bridge MCP server (standalone, for third-party MCP clients). Same toolset as the Pi native extension: send_message/photo/video/document, queue + session + relay management.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "node-fetch": "^3.3.2",
    "better-sqlite3": "^11.0.0"
  }
}